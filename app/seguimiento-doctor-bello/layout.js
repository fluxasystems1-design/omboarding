const title = "Seguimiento — Dr. Leonardo Bello";

export const metadata = {
  title,
  alternates: { canonical: "/seguimiento-bello" },
};

export default function SeguimientoDoctorBelloRedirectLayout({ children }) {
  return children;
}
